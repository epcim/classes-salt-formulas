parameters:
  rabbitmq:
    server:
      host:
        '/openstack':
          policies:
          - name: ha-all
            pattern: '.'
            definition: '{"ha-mode":"all", "ha-sync-mode":"automatic"}'
            priority: 0
